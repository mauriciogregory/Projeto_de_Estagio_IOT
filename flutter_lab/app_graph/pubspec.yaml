name: app_graph
description: "Projeto IoT para consumo de uma API."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=3.3.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  http: ^1.2.1
  fl_chart: ^0.68.0
  cupertino_icons: ^1.0.6
  flutter_secure_storage: ^9.2.2
  shared_preferences: 
  jwt_decoder: 


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/

